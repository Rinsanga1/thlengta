<header class="nav">
  <div class="container nav-inner">
    <a class="brand" href="/index.html">
      <span>
        <img src="/assets/img/thlengtalogo.png" alt="Thlengta logo" width="80px"/>
      </span>
      <span class="small">
        <img src="/assets/img/thlengtalogotext.png" atl="Thlengta logo type" width="195px"/>
      </span>
      <span class="small">
        <img src="/assets/img/com.png" atl="Thlengta logo type" width="75px" style="margin-left: -10px"/>
      </span>
    </a>

    <nav class="nav-links">
      <a class="btn" href="/index.html">Back</a>
    </nav>
  </div>
</header>

<section class="section" style="padding-top:56px;">
  <div class="card pad super" style="max-width:920px;margin:0 auto;">
    <div class="topbar" style="margin-bottom:14px;">
      <div>
        <div class="h-eyebrow">
          <span class="chip">Thlengta</span>
          <span class="chip">Signup</span>
        </div>
        <h2 style="margin:10px 0 0;">Create an Account</h2>
        <div class="small" style="margin-top:6px;">
          Get ready to simplify your attendance tracking!
        </div>

        <div class="small" style="margin-top:10px;">
          Selected plan: <b><%= (plan || "standard").toUpperCase() %></b>
        </div>
      </div>
    </div>

    <div class="notice" style="margin:12px 0; background:#f8faf9;">
      <div style="font-weight:900; display:flex; align-items:center; gap:10px; flex-wrap:wrap;">
        <span>Approval flow</span>
        <img src="/assets/img/beta.png" width="50px" alt="Beta"/>
      </div>
      <div class="small" style="margin-top:6px; line-height:1.4;">
        After you register, your account will be pending approval. Once approved, you can login and start creating stores and employees.
        We are doing this because we are at a beta stage of development and want to ensure quality service.
        Thank you for your understanding!
      </div>
    </div>

    <% if (error) { %>
      <div class="error"><%= error %></div>
    <% } %>

    <form class="form" method="post" action="/register" style="margin-top:14px;">
      <input type="hidden" name="plan" value="<%= plan || 'standard' %>">

      <!-- Desktop: 2 columns. Mobile: stacks -->
      <div style="display:grid; grid-template-columns: 1fr 1fr; gap:14px; align-items:start;">
        <div class="card pad" style="background:#fff;">
          <h3 style="margin:0 0 10px;">Personal info</h3>

          <div class="field">
            <label>Name</label>
            <input type="text" name="name" required placeholder="Your full name" autocomplete="name" />
          </div>

          <div class="field">
            <label>Phone</label>
            <input type="text" name="phone" required placeholder="Phone number" autocomplete="tel" />
          </div>

          <div class="field">
            <label>Address</label>
            <input type="text" name="address" required placeholder="Business / store address" autocomplete="street-address" />
          </div>

          <div class="notice" style="margin-top:12px; background:#eaf6f0;">
            <div style="font-weight:900;">Tip</div>
            <div class="small" style="margin-top:6px;">
              Use your main business contact details. This helps faster approval.
            </div>
          </div>
        </div>

        <div class="card pad" style="background:#fff;">
          <h3 style="margin:0 0 10px;">Account info</h3>

          <div class="field">
            <label>Email</label>
            <input type="email" name="email" required placeholder="you@example.com" autocomplete="email" />
          </div>

          <div class="field">
            <label>Password</label>
            <input
              type="password"
              name="password"
              minlength="8"
              required
              placeholder="Minimum 8 characters"
              autocomplete="new-password"
            />
            <div class="small">Use a strong, unique password.</div>
          </div>

          <button class="btn primary full" type="submit">
            Submit for Approval
          </button>

          <div class="small" style="margin-top:10px; line-height:1.4;">
            By submitting, you agree to be contacted for verification if needed.
          </div>
        </div>
      </div>

      <!-- Mobile stack -->
      <style>
        @media (max-width: 820px){
          .card.pad.super form > div[style*="grid-template-columns"]{
            grid-template-columns: 1fr !important;
          }
          /* slightly tighter padding feel on mobile */
          .notice{ line-height: 1.45; }
        }
      </style>
    </form>
  </div>
</section>
